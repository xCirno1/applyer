## Summary

<!-- What does this PR do, and why? -->

## Related issue

<!-- Closes #... , if applicable -->

## How was this tested?

<!-- e.g. npm run typecheck / npm run lint / npm run smoke:mcp, manual testing steps -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] `npm run smoke:mcp` passes (if MCP tools were touched)
- [ ] UI changes follow the design conventions in `CLAUDE.md` (dense controls, no icon
      backgrounds, toast/modal instead of native dialogs, etc.)
- [ ] Tested manually in the running app
